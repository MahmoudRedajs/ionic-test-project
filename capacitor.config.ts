import { CapacitorConfig } from '@capacitor/cli';

const config: CapacitorConfig = {
  appId: 'org.mahmoudreda.testemplyees',
  appName: 'emplyess',
  webDir: 'www',
  bundledWebRuntime: false
};

export default config;
